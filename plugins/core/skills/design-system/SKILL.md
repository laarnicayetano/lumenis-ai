---
name: design-system
description: The company-wide Lumenis design system — color tokens, typography, spacing, and core component conventions. Use when creating or reviewing any visual output (landing pages, slides, graphics, UI), when a task asks about brand colors, fonts, or component styling, or when checking a design matches Lumenis standards. For product-specific visual rules that differ from the company default, use that product's design skill, which overrides these values.
---

# Lumenis design system

Global baseline. When a product design skill is loaded, its overrides win.

## Color
See `references/tokens.md` for the full token list; palette values in
`assets/palette.json` (reference by path; don't paste the whole file into a reply).

## Typography
See `references/tokens.md` for the type scale, families, and weights.

## Components
See `references/components.md` for buttons, cards, and layout conventions.

## How to use this
- Prefer tokens over hard-coded values.
- If a product design skill is loaded, its overrides take precedence.
