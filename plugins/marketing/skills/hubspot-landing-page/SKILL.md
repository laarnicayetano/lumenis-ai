---
name: hubspot-landing-page
description: Build HubSpot landing pages using the bundled builder script. Use when the user wants to create or scaffold a HubSpot landing page, generate landing-page markup for HubSpot, or run the landing-page build workflow. Runs the bundled build script.
---

# HubSpot landing-page builder

## Usage
```
python scripts/build.py --name "<page name>" [--template default]
```

## Notes
- Fill in HubSpot API/config details in the script before first use.
- Pair with the relevant product context skill so copy and design match.
