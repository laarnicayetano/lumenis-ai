## Adding a product

Ask (with the `marketing` plugin installed): "set up a skill for a new
product." The `new-product-skill` generator writes a single context skill
into `core/skills/product-<slug>/`, matching `product-example`. Then say
"ship this" to publish it.
